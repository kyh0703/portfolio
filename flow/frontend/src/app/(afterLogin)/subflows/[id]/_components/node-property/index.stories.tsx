import { Meta, StoryObj } from '@storybook/react'
import FlowRightSidebar from '.'

const meta: Meta<typeof FlowRightSidebar> = {
  component: FlowRightSidebar,
  title: 'flows/FlowRightSidebar',
}

export default meta
type Story = StoryObj<typeof FlowRightSidebar>

export const Normal: Story = {}
